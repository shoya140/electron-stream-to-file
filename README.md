# Electron Stream to File

This is a sample project of storing a data stream as a file by using Electron. Data will be saved in `~/electron-stream-to-file/`.

## Getting Started

```
$ npm install
$ npm run dev
```
